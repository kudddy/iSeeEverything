from .prediction import PredictionHandler
from .comments import GetCommentHandler

# HANDLERS = (CheckToken, CreateToken, UploadFile, PredictionHandler)


HANDLERS = (PredictionHandler, GetCommentHandler,)
